/**
 * Database seed script.
 *
 * Responsibility (idempotent — safe to re-run via upsert on fixed emails):
 *  - Create a few sample users, each with a funded wallet.
 *  - Create one upcoming contest.
 *  - Create a small avatar-item catalog.
 *
 * Run with: `npm run seed`
 */
